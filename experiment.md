There are some questions of preference.
We generally want to see the filter direction = inbound
and counts to be done on distinct(id).

This information is captured in the metrics of the semantic view. 